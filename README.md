
# Adaptive & Causal Digital Twin — Upgraded Package
This package contains a full repo scaffold with:
- Multi-stage Dockerfiles (dev, full, slim)
- docker-compose.full.yml and docker-compose.dev.yml
- FastAPI backend stubs and notebooks
- Jupyter integration
- GitHub Actions CI for tests + image builds
- MkDocs docs + GitHub Pages workflow
- Helm chart scaffold
- gRPC proto and stubs placeholder
- Terraform starter templates
- Semver tagging script and release workflow
- devcontainer for VS Code / Codespaces

Unzip and follow the README sections below to initialize repo and run locally.
